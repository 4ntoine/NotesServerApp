The `app-api` module exposes application interface - use cases and data structures (DTOs).
This is helpful even for non-developers (eg. managers) to have an idea on what's the data and application functions are.
